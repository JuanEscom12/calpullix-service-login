spring:
  application: 
    name: calpullix-service-login
  config:
    name: '${spring.application.name}'
  cloud:
    config:
      url: http://localhost:8888
      username: root
      password: s3cr3t
  profiles:
    active: default

server:
  port: 9990